# SBOM Flask

A Flask web application that converts Software Bill of Materials (SBOM) JSON files into formatted, human-readable PDF documents. The application accepts SBOM files in JSON format (typically SPDX format) and generates professionally formatted PDF reports containing document information, packages, files, and relationships.
